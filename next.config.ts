import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  basePath: '/Application-Form',
  assetPrefix: '/Application-Form/',
};

export default nextConfig;
